# Historical agent spec

Deprecated on 2026-04-23.

This file name is preserved so the older agent set remains indexed, but the original instructions are intentionally retired from active use.

Reasons this spec is unsafe for current execution:

- it depends on `docs/plan.md` and `taskboard.md`
- it assumes the older phase-retro workflow is still active
- it is coupled to the pre-rewrite orchestrator process

Do not dispatch from this file. Reviewer follow-up now lives under `agents/reviewer.md`.
