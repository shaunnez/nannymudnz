# Historical agent spec

Deprecated on 2026-04-23.

This file name is preserved so the older agent set remains indexed, but the original instructions are intentionally retired from active use.

Reasons this spec is unsafe for current execution:

- it targets an older repo layout
- it assumes `pnpm`
- it points at stale paths such as `packages/client/*`, `packages/server/src/sim/*`, and `lore/*.json`
- it references older control docs that are not active in this repo

Do not dispatch from this file. Use `agents/dev.md` instead.
